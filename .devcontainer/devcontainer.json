{
    "name": "Agentic Modernization",
    // Universal Ubuntu image with Ora2Pg for Oracle→PostgreSQL migrations
    // Ships with Java 21, Maven, Terraform, Azure CLI, Docker, and Ora2Pg
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    // VS Code extensions pre-installed inside the container
    "customizations": {
        "vscode": {
            "extensions": [
                // Java support
                "vscjava.vscode-java-pack",
                "vscjava.vscode-maven",
                // GitHub Copilot
                "GitHub.copilot",
                "GitHub.copilot-chat",
                "ms-dotnettools.upgrade-agent",
                // Terraform
                "hashicorp.terraform",
                // Azure
                "ms-azuretools.vscode-azureresourcegroups",
                // Docker
                "ms-azuretools.vscode-docker",
                // Misc
                "redhat.vscode-xml",
                "vscjava.migrate-java-to-azure"
            ],
            "settings": {
                "java.configuration.updateBuildConfiguration": "automatic",
                "editor.formatOnSave": true,
                "terminal.integrated.defaultProfile.linux": "bash"
            }
        }
    },
    // universal:2 image uses "codespace" as the default non-root user
    "remoteUser": "codespace",
    // Forward the port Jetty/Tomcat listens on
    "forwardPorts": [
        8080
    ],
    "portsAttributes": {
        "8080": {
            "label": "Agentic Modernization",
            "onAutoForward": "openBrowserOnce"
        }
    },
    // Useful lifecycle commands
    "postCreateCommand": "git submodule update --init --recursive && curl -fsSL https://raw.githubusercontent.com/microsoft/modernize-cli/main/scripts/install.sh | sh && source ~/.bashrc && mvn --version && terraform --version && az --version && modernize --version",
    "mounts": []
}